---
import dk from "../dk.svg";
import Layout from "@layouts/Layout.astro";
import Container from "@layouts/Container.astro";
import { getEntry } from "astro:content";
const team = await getEntry("pages", "team");
const { Content, headings } = await team.render();
---

<Layout title="Team" description="Test">
  <div
    class="flex flex-col items-center justify-center prose-li:pr-2 hover:prose-a:opacity-50 text-green prose-headings:text-green prose-li:inline prose-ul:inline prose-li:text-5xl prose-li:p-0 prose-ul:p-0 marker:none prose-a:text-green prose-li:decoration-green marker:hidden prose-a:no-underline prose-a:font-bold prose-li:text-green"
  >
    <Container>
      <Content />
    </Container>
  </div>
</Layout>
